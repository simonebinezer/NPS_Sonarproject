pipeline {
  agent none
  stages {

    stage("build & SonarQube Analysis") {
      agent any
      steps {
        withSonarQubeEnv('Sonar_Qube') {
          bat 'mvn clean package sonar:sonar'
        }
      }
    }
  }
}
